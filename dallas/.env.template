# Iowa Property Tax Scraper — Environment Config
# Copy this to .env and fill in your values

# PostgreSQL connection
DB_HOST=localhost
DB_PORT=5432
DB_NAME=iowa_propertytax
DB_USER=postgres
DB_PASS=

# File paths
SNAPSHOT_DIR=./snapshots/dallas
BEACON_PROFILE_DIR=./beacon_profile

# CSV output (used when --csv flag is passed instead of writing to DB)
CSV_OUTPUT_DIR=./csv_output
